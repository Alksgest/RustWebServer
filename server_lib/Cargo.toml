[package]
name = "server_lib"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[dependencies.mongodb]
version = "2.0.0-beta.2"
default-features = false
features = ["sync"]
